{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
	"scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start-app",
    "watch": "lerna run watch --stream"
  }
}
